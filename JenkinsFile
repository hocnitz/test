pipeline {
    agent {
        docker {
            image 'qnib/pytest'
        }
    }
    stages {
        stage('Run tests') {
            steps {
                sh 'python3 -m unittest tests.py'
            }
        }
    }
}