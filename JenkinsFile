pipeline {
    agent any
    stages {
        // stage('Checkout') {
        //     steps {
        //         git 'https://github.com/hocnitz/test.git'
        //     }
        // }
        stage('Install dependencies') {
            steps {
                sh 'python3 -m pip install -r requirements.txt'
            }
        }
        stage('Run tests') {
            steps {
                sh 'python3 -m unittest tests.py'
            }
        }
    }
}